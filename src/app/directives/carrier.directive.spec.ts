import { CarrierDirective } from './carrier.directive';

describe('CarrierDirective', () => {
  it('should create an instance', () => {
    const directive = new CarrierDirective();
    expect(directive).toBeTruthy();
  });
});
